display_name: "WAL Crash Recovery Testing"
short_description: "Design crash tests for before/after WAL flush, TxCommit, dirty page flush, manifest switch, Map delta, WAL truncation, and recovery reports. Use when working on C5 validation."
default_prompt: "Use the WAL Crash Recovery Testing skill to analyze the current Andromeda task and return a bounded, validated result."
