display_name: "PostToolUse Audit"
short_description: "Audit tool outputs after execution and inject follow-up context for validation, tests, or cleanup. Use after file edits, shell commands, searches, or validation"
default_prompt: "Use the PostToolUse Audit skill to analyze the current Andromeda task and return a bounded, validated result."
