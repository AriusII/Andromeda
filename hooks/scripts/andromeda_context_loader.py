#!/usr/bin/env python3
from andromeda_common import allow

allow("Andromeda context loaded: preserve Procedure-only execution, QUIC + Protobuf without gRPC, SRPL strict semantics, WAL-before-visible-commit, and recovery-first design.")
