Livrables générés :

- [Markdown — Andromeda Rust 2026 Transverse Architecture Plan](sandbox:/mnt/data/andromeda_rust_2026_transverse_architecture_plan.md)
- [PDF — Andromeda Rust 2026 Transverse Architecture Plan](sandbox:/mnt/data/andromeda_rust_2026_transverse_architecture_plan.pdf)

Le PDF a été rendu et vérifié visuellement sur **50 pages** après génération. Le document est rédigé en **anglais américain**, avec une structure de documentation technique proche de Microsoft Learn.

J’ai intégré les sources internes Andromeda : doctrine, architecture, SRPL, transaction/WAL/MVCC/storage/recovery, QUIC/RPC/security/HA/DR, optimizer/statistics/hardware, moteur de stockage sécurisé, CPU/GPU Rust, et corpus scientifique relationnel. Les invariants principaux repris sont notamment : surface applicative sans SQL ad hoc, exécution par Procedure cataloguée, contrat typé/hashé/versionné, transaction implicite, WAL durable avant commit visible, vérité reconstruite par snapshot froid + WAL, GPU hors chemin commit/recovery, et décisions critiques observables. Ces invariants sont présents dans l’index consolidé, le document d’architecture, le document transaction/storage, et le document optimizer/hardware fileciteturn0file25 fileciteturn0file23 fileciteturn0file22 fileciteturn0file26

J’ai aussi croisé l’état réel du dépôt GitHub `AriusII/Andromeda`, branche `main`. Le dépôt déclare actuellement un workspace Rust 2024 avec `resolver = "3"` et `rust-version = "1.85"` ; le document final recommande donc une politique à deux niveaux : conserver le MSRV du dépôt à `1.85` tant qu’un ADR ne le modifie pas, tout en utilisant Rust `1.95.0` comme baseline stable 2026 de développement/validation. Le dépôt expose aussi les crates principales `andromeda-bench`, `andromeda-cli`, `andromeda-catalog`, `andromeda-core`, `andromeda-exec`, `andromeda-observe`, `andromeda-proto`, `andromeda-quic`, `andromeda-srpl`, `andromeda-storage`, et `andromeda-tx` fileciteturn6file0. Rust 1.95.0 a été vérifié sur la source officielle Rust Blog comme release stable publiée le 16 avril 2026 ([blog.rust-lang.org](https://blog.rust-lang.org/2026/04/16/Rust-1.95.0/?utm_source=chatgpt.com)), et la règle Rust 2024 `edition = "2024"` -> resolver 3 a été vérifiée sur l’Edition Guide officiel ([doc.rust-lang.org](https://doc.rust-lang.org/stable/edition-guide/rust-2024/cargo-resolver.html?utm_source=chatgpt.com)).

Le document prend également en compte que le dépôt est aujourd’hui un **recoverable vertical prototype** avec fondation modulaire avancée, mais pas encore un SGBDRT de production, un serveur QUIC applicatif complet, ni un moteur de stockage durable complet fileciteturn8file0. La roadmap GitHub impose le chemin prioritaire `Procedure -> Catalog -> SRPL IR -> Admission -> Transaction -> WAL -> Heap/Page -> Recovery -> ResultStream`, et le document final reprend cet ordre comme colonne vertébrale de la feuille de route fileciteturn9file0.