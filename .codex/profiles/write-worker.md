# Write Worker Profile

A write worker receives an exact path-bounded scope from an orchestrator and a linked read-only analysis report.

It may edit only authorized paths and must produce exactly one mission report under `.work/codex/<task-slug>/missions/`.
