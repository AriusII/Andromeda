# Andromeda Codex configuration
# Regenerated by .codex/scripts/repair_codex_agent_registry.py
sandbox_mode = "workspace-write"
include_environment_context = true
include_permissions_instructions = true

[features]
child_agents_md = true
hooks = true
multi_agent = true
plugins = true
skills = true

[agent_runtime]
interrupt_message = true
job_max_runtime_seconds = 3600
max_depth = 2
max_threads = 60

[tui.model_availability_nux]
"gpt-5.5" = 1

[mcp_servers.rust-lsp-plugin]
command = "rust-lsp-plugin"

# Agent registry
[agents.agent-registry-maintainer]
config_file = "agents/agent-registry-maintainer.toml"
description = "Maintains .codex/config.toml agent entries and .codex/agents TOML coherence."

[agents.analytics-gpu-architect]
config_file = "agents/analytics-gpu-architect.toml"
description = "Owns off-critical-path analytics, SIMD/GPU batch work, and GPU safety boundaries. Use for GPU-assisted statistics, analytical maps, vector/statistical workloads, and GPU exclusion from commit path."

[agents.andromeda-architect]
config_file = "agents/andromeda-architect.toml"
description = "System-level architecture reviewer for Andromeda doctrine, engine boundaries, invariants, and module separation."

[agents.andromeda-chief-architect]
config_file = "agents/andromeda-chief-architect.toml"
description = "Owns global coherence across engines, planes, contracts, and roadmap. Use for broad architecture decisions, cross-engine tradeoffs, and final consolidation."

[agents.backup-restore-forensic-engineer]
config_file = "agents/backup-restore-forensic-engineer.toml"
description = "Implements backup, restore, PITR, forensic startup, immutable retention, and restore testing."

[agents.benchmark-evidence-architect]
config_file = "agents/benchmark-evidence-architect.toml"
description = "Owns predictive evidence scenarios, workload realism, scoring, and benchmark governance. Use for benchmark design, ScenarioEvidence, and evidence validation."

[agents.binary-format-specifier]
config_file = "agents/binary-format-specifier.toml"
description = "Writes deterministic binary format specifications for pages, frames, WAL, and manifests. Use when a format requires offsets, endian rules, version fields, checksums, or compatibility policy."

[agents.buffer-pool-engineer]
config_file = "agents/buffer-pool-engineer.toml"
description = "Designs BufferPool pinning, dirty tracking, eviction, checkpoint cooperation, read-ahead, and metrics."

[agents.catalog-contract-engine-architect]
config_file = "agents/catalog-contract-engine-architect.toml"
description = "Owns canonical catalog, object versions, contract hashes, and definition batches. Use for system database, namespaces, object contracts, catalog history, and import batches."

[agents.catalog-modelization-governor]
config_file = "agents/catalog-modelization-governor.toml"
description = "Governs catalog, modelization, DefinitionBatch, object lifecycles, and versioned policies."

[agents.ci-quality-gate-engineer]
config_file = "agents/ci-quality-gate-engineer.toml"
description = "Implements CI gates and local scripts for Rust and Codex tooling validation."

[agents.codebase-cleanup-commander]
config_file = "agents/codebase-cleanup-commander.toml"
description = "Coordinates broad code cleanup, dead code removal, orphan detection, refactor slices, and validation commands."

[agents.codex-routing-architect]
config_file = "agents/codex-routing-architect.toml"
description = "Defines agent and skill routing strategies for Codex based on request type, subsystem, risk, and deliverable."

[agents.codex-skill-maintainer]
config_file = "agents/codex-skill-maintainer.toml"
description = "Creates, updates, validates, and forward-tests Codex skills with progressive disclosure and precise triggers."

[agents.context-intake-analyst]
config_file = "agents/context-intake-analyst.toml"
description = "Builds a precise understanding of user intent, project context, available sources, and uncertainty before execution."

[agents.context-pack-builder]
config_file = "agents/context-pack-builder.toml"
description = "Creates reusable context packs for handoffs, subagents, implementation plans, and prompt templates."

[agents.definitionbatch-implementer]
config_file = "agents/definitionbatch-implementer.toml"
description = "Implements DefinitionBatch parsing, dependency sorting, DryRun, validation, WAL coverage, and rollback."

[agents.dependency-supply-chain-auditor]
config_file = "agents/dependency-supply-chain-auditor.toml"
description = "Read-only auditor for Cargo dependencies, licenses, advisories, features, and dependency graph risk."

[agents.devex-rust-tooling-agent]
config_file = "agents/devex-rust-tooling-agent.toml"
description = "Implements developer experience tooling for Rust quality gates, scripts, xtask, reports, and Codex validation."

[agents.doctrine-guardian]
config_file = "agents/doctrine-guardian.toml"
description = "Checks all proposals against non-negotiable Andromeda invariants. Use before accepting architecture, protocol, SRPL, storage, or security changes."

[agents.documentation-architect]
config_file = "agents/documentation-architect.toml"
description = "Owns Microsoft-style documentation, terminology consistency, information architecture, and deliverable polish. Use for documentation pages, README, architecture documents, and review edits."

[agents.documentation-consistency-editor]
config_file = "agents/documentation-consistency-editor.toml"
description = "Read-only documentation consistency agent for Andromeda and Codex tooling documents."

[agents.enterprise-readiness-agent]
config_file = "agents/enterprise-readiness-agent.toml"
description = "Read-only enterprise readiness reviewer for supportability, auditability, recoverability, operations, and release gates."

[agents.file-size-split-enforcer]
config_file = "agents/file-size-split-enforcer.toml"
description = "Finds large Rust files and proposes responsibility-based splits with tests and reexports."

[agents.formal-invariants-agent]
config_file = "agents/formal-invariants-agent.toml"
description = "Read-only formal invariants agent for set theory, relational algebra, transaction invariants, and safety proofs."

[agents.github-implementation-planner]
config_file = "agents/github-implementation-planner.toml"
description = "Plans repository changes from GitHub context into safe file edits, tests, and review packets."

[agents.github-pr-reviewer]
config_file = "agents/github-pr-reviewer.toml"
description = "Read-only GitHub PR reviewer for Rust, Andromeda architecture, tests, and policy compliance."

[agents.hadr-backup-architect]
config_file = "agents/hadr-backup-architect.toml"
description = "Owns single-primary replication, quorum, WAL shipping, backup, restore, PITR, and forensic startup. Use for HA/DR, backup, restore, failover, quorum, and disaster recovery planning."

[agents.hadr-failover-engineer]
config_file = "agents/hadr-failover-engineer.toml"
description = "Implements single-primary HA/DR, WAL shipping, quorum, fencing, promotion, epochs, and cluster manifests."

[agents.hadr-operations-runbook-agent]
config_file = "agents/hadr-operations-runbook-agent.toml"
description = "HA/DR, backup, restore, PITR, forensic startup, and operations runbook specialist."

[agents.hardware-rust-performance-engineer]
config_file = "agents/hardware-rust-performance-engineer.toml"
description = "Read-only hardware/Rust performance reviewer for x64/arm64/SIMD/GPU/NVMe/HDD policies."

[agents.hook-policy-implementer]
config_file = "agents/hook-policy-implementer.toml"
description = "Implements and hardens Codex hooks and hook scripts for Andromeda workflows."

[agents.hooks-governance-auditor]
config_file = "agents/hooks-governance-auditor.toml"
description = "Read-only auditor for Codex hook policies, hook scripts, matcher usage, failure modes, and user friction."

[agents.map-analytics-summarizability-reviewer]
config_file = "agents/map-analytics-summarizability-reviewer.toml"
description = "Read-only reviewer for Maps, analytics grain, summarizability, refresh policy, and GPU boundaries."

[agents.map-maintenance-engineer]
config_file = "agents/map-maintenance-engineer.toml"
description = "Implements Map refresh modes, delta logs, staleness policies, validation, and map/stat publication."

[agents.mcp-awareness-integrator]
config_file = "agents/mcp-awareness-integrator.toml"
description = "Maintains Codex instructions for using rust-lsp and rust-rover-ide MCP tools when available, without hard dependency."

[agents.mega-refactor-coordinator]
config_file = "agents/mega-refactor-coordinator.toml"
description = "Coordinates large refactors with behavior locks, strangler patterns, agent handoffs, and rollback plans."

[agents.modelization-catalog-governor]
config_file = "agents/modelization-catalog-governor.toml"
description = "Read-only governor for Modelization artifacts, DefinitionBatch imports, catalog descriptors, and versioning."

[agents.mvcc-isolation-engineer]
config_file = "agents/mvcc-isolation-engineer.toml"
description = "Designs MVCC visibility, isolation policies, long-reader handling, version GC, and anomaly documentation."

[agents.observability-forensic-architect]
config_file = "agents/observability-forensic-architect.toml"
description = "Owns traces, metrics, decision logs, recovery evidence, and forensic reports. Use for DecisionTrace, ProcedureInvocationTrace, RecoveryTrace, and audit-friendly output."

[agents.observability-forensic-engineer]
config_file = "agents/observability-forensic-engineer.toml"
description = "Implements trace schemas, metrics, RecoveryReport, DecisionTrace, audit ledgers, and forensic evidence."

[agents.optimizer-plan-engineer]
config_file = "agents/optimizer-plan-engineer.toml"
description = "Implements optimizer plan candidates, physical operators, cost model, plan cache, plan classes, and DecisionTrace."

[agents.optimizer-statistics-architect]
config_file = "agents/optimizer-statistics-architect.toml"
description = "Owns cost/evidence planning, PlanClass, statistics versions, histograms, and plan stability. Use for plan cache, statistics, cardinality, skew, and evidence-driven optimization."

[agents.optimizer-statistics-critic]
config_file = "agents/optimizer-statistics-critic.toml"
description = "Read-only critic for optimizer, statistics, evidence, learned components, and plan stability."

[agents.orphan-detection-specialist]
config_file = "agents/orphan-detection-specialist.toml"
description = "Finds orphan files, modules, dependencies, features, docs, examples, tests, and scripts."

[agents.performance-engineer]
config_file = "agents/performance-engineer.toml"
description = "Owns performance budgets, benchmark interpretation, cache behavior, IO pressure, and regression risk. Use for CPU/NVMe/QUIC/storage performance analysis and budget proposals."

[agents.procedure-contract-implementer]
config_file = "agents/procedure-contract-implementer.toml"
description = "Implements ProcedureContract descriptors, canonicalization, compatibility, and contract validation."

[agents.procedure-contract-reviewer]
config_file = "agents/procedure-contract-reviewer.toml"
description = "Read-only reviewer for ProcedureContract, ContractHash, compatibility, permissions, and RPC shapes."

[agents.procedure-store-engineer]
config_file = "agents/procedure-store-engineer.toml"
description = "Implements Procedure Store invocation history, feedback, metrics, regression detection, and audit correlation."

[agents.prompt-library-curator]
config_file = "agents/prompt-library-curator.toml"
description = "Maintains .codex/prompts templates and aligns them with agents, skills, and Andromeda doctrine."

[agents.prompt-refinement-specialist]
config_file = "agents/prompt-refinement-specialist.toml"
description = "Rewrites broad user requests into precise Codex work orders and prompt templates without losing intent."

[agents.promptops-curator-agent]
config_file = "agents/promptops-curator-agent.toml"
description = "Owns prompts, instruction pack quality, agent-skill registry coherence, and hook governance. Use when editing this AI operating pack."

[agents.protobuf-contract-architect]
config_file = "agents/protobuf-contract-architect.toml"
description = "Owns .proto contracts, compatibility, schema evolution, result metadata, and StructuredObject payload shapes. Use for contract generation, schema review, and protobuf compatibility rules."

[agents.qa-crash-recovery-test-designer]
config_file = "agents/qa-crash-recovery-test-designer.toml"
description = "Designs deterministic crash/recovery tests for WAL, storage, catalog, maps, and manifest publication."

[agents.quic-rpc-contract-auditor]
config_file = "agents/quic-rpc-contract-auditor.toml"
description = "Read-only auditor for QUIC/RPC frame contracts, surfaces, metadata-before-payload, errors, and backpressure."

[agents.quic-rpc-implementation-engineer]
config_file = "agents/quic-rpc-implementation-engineer.toml"
description = "Implements QUIC/RPC frames, session state, typed payloads, ResultStream, errors, and backpressure."

[agents.quic-transport-architect]
config_file = "agents/quic-transport-architect.toml"
description = "Owns QUIC surfaces, streams, flow control, backpressure, session behavior, and RPC dispatch boundaries. Use for network surface, stream mapping, backpressure, connection lifecycle, and transport errors."

[agents.release-governance-agent]
config_file = "agents/release-governance-agent.toml"
description = "Owns release readiness, versioning, artifact checks, and publication gates. Use before tagging, publishing documentation, or freezing specs."

[agents.release-readiness-coordinator]
config_file = "agents/release-readiness-coordinator.toml"
description = "Coordinates release readiness evidence across Rust, Codex tooling, C4/C5 components, docs, and validation."

[agents.research-synthesizer]
config_file = "agents/research-synthesizer.toml"
description = "Turns academic, standards, and vendor sources into project-safe recommendations. Use for modern research, source triangulation, and frontier versus consensus distinction."

[agents.risk-decision-manager]
config_file = "agents/risk-decision-manager.toml"
description = "Maintains risk register, decision records, rejected alternatives, and open issues. Use for ADRs, risk reviews, decision cleanup, and project governance."

[agents.roadmap-master-orchestrator]
config_file = "agents/roadmap-master-orchestrator.toml"
description = "Central roadmap-following agent for large Andromeda Codex work. Coordinates objectives, task graphs, sub-agent routing, checkpoints, and final integration."

[agents.roadmap-scope-controller]
config_file = "agents/roadmap-scope-controller.toml"
description = "Read-only scope controller that detects scope creep, missing acceptance criteria, and unsafe expansion beyond the requested roadmap."

[agents.roadmap-task-decomposer]
config_file = "agents/roadmap-task-decomposer.toml"
description = "Breaks large roadmap prompts into executable units, ordered milestones, dependencies, and validation gates."

[agents.rust-allocation-optimizer]
config_file = "agents/rust-allocation-optimizer.toml"
description = "Reviews allocations, clone patterns, buffer growth, and memory pressure in Rust hot paths."

[agents.rust-architecture-splitter]
config_file = "agents/rust-architecture-splitter.toml"
description = "Splits Rust architecture by workspace, crates, modules, and responsibilities."

[agents.rust-async-concurrency-engineer]
config_file = "agents/rust-async-concurrency-engineer.toml"
description = "Designs Rust async, cancellation, shutdown, backpressure, and concurrency-safe runtime boundaries."

[agents.rust-binary-codec-engineer]
config_file = "agents/rust-binary-codec-engineer.toml"
description = "Implements explicit binary codecs for WAL, pages, manifests, segments, RPC frames, and contracts."

[agents.rust-cleanup-refactor-architect]
config_file = "agents/rust-cleanup-refactor-architect.toml"
description = "Leads broad Rust cleanup and refactor efforts while preserving behavior and tests."

[agents.rust-crate-graph-architect]
config_file = "agents/rust-crate-graph-architect.toml"
description = "Designs and audits the Cargo crate graph and dependency direction."

[agents.rust-dead-code-excavator]
config_file = "agents/rust-dead-code-excavator.toml"
description = "Finds and removes dead Rust code, unused modules, stale examples, and obsolete paths."

[agents.rust-error-model-engineer]
config_file = "agents/rust-error-model-engineer.toml"
description = "Designs typed error models for Rust crates and protocol boundaries."

[agents.rust-gpu-batch-engineer]
config_file = "agents/rust-gpu-batch-engineer.toml"
description = "Implements GPU batch acceleration outside commit/recovery paths with cancellation and CPU fallback."

[agents.rust-memory-safety-auditor]
config_file = "agents/rust-memory-safety-auditor.toml"
description = "Read-only auditor for Rust memory safety, aliasing, lifetimes, unsafe boundaries, and panic risks."

[agents.rust-module-boundary-surgeon]
config_file = "agents/rust-module-boundary-surgeon.toml"
description = "Refactors module boundaries, visibility, reexports, and tests around a single responsibility."

[agents.rust-performance-optimizer]
config_file = "agents/rust-performance-optimizer.toml"
description = "Optimizes Rust code only with measurement, profiling, hypotheses, and regression gates."

[agents.rust-simd-kernel-engineer]
config_file = "agents/rust-simd-kernel-engineer.toml"
description = "Implements CPU SIMD kernels with feature detection, fallback, tests, and benchmarks."

[agents.rust-unsafe-audit-agent]
config_file = "agents/rust-unsafe-audit-agent.toml"
description = "Read-only unsafe Rust audit agent for storage, WAL, SIMD, FFI, and lock-free code."

[agents.rust-workspace-architect]
config_file = "agents/rust-workspace-architect.toml"
description = "Designs workspace layout, crate boundaries, features, and build profiles. Use for Rust project structure, Cargo workspace, module dependencies, and tooling."

[agents.security-iam-auditor]
config_file = "agents/security-iam-auditor.toml"
description = "Read-only security/IAM auditor for identity, permissions, policies, audit, break-glass, and least privilege."

[agents.security-iam-threat-modeler]
config_file = "agents/security-iam-threat-modeler.toml"
description = "Read-only threat-modeling agent for IAM, RPC, admin, backup, cluster, and storage tampering scenarios."

[agents.security-implementation-engineer]
config_file = "agents/security-implementation-engineer.toml"
description = "Implements IAM, permission evaluation, policy checks, audit traces, certificate identity, and break-glass enforcement."

[agents.source-grounding-researcher]
config_file = "agents/source-grounding-researcher.toml"
description = "Read-only researcher that cross-checks claims against official sources, repository evidence, and Andromeda documents."

[agents.srpl-compiler-ir-architect]
config_file = "agents/srpl-compiler-ir-architect.toml"
description = "Owns parsing, binding, typed AST, IR lowering, semantic validation, and plan preparation. Use for compiler pipeline, canonical IR, semantic hashes, and plan cache keys."

[agents.srpl-diagnostics-engineer]
config_file = "agents/srpl-diagnostics-engineer.toml"
description = "Maintains SRPL diagnostic codes, compiler messages, and fix guidance."

[agents.srpl-language-designer]
config_file = "agents/srpl-language-designer.toml"
description = "Designs SRPL syntax and user-facing semantics. Use for SRPL syntax, cardinality, errors, procedure shape, and language restrictions."

[agents.srpl-language-specifier]
config_file = "agents/srpl-language-specifier.toml"
description = "Read-only SRPL language specification agent for syntax, semantics, restrictions, diagnostics, and IR."

[agents.srpl-parser-binder-engineer]
config_file = "agents/srpl-parser-binder-engineer.toml"
description = "Implements SRPL parsing, binding, type checking, cardinality validation, and semantic IR preparation."

[agents.statistics-engineer]
config_file = "agents/statistics-engineer.toml"
description = "Implements statistics collection, histograms, skew, StatsVersion, publication, validation, and CPU/GPU separation."

[agents.storage-engine-architect]
config_file = "agents/storage-engine-architect.toml"
description = "Owns pages, extents, segments, HotStore, ColdStore, BufferPool, and manifest switching. Use for storage layout, hot/cold placement, page format, and cold segment invariants."

[agents.storage-engine-page-layout-auditor]
config_file = "agents/storage-engine-page-layout-auditor.toml"
description = "Read-only auditor for page layouts, segment formats, manifests, and storage invariants."

[agents.storage-segment-format-engineer]
config_file = "agents/storage-segment-format-engineer.toml"
description = "Implements segmented storage, manifests, root pointers, SegmentIndex, cold snapshots, and hot/cold merge policy."

[agents.test-verification-architect]
config_file = "agents/test-verification-architect.toml"
description = "Designs layered verification strategy: unit, property, fuzz, Miri, Loom, crash/recovery, and CI gates."

[agents.transaction-kernel-architect]
config_file = "agents/transaction-kernel-architect.toml"
description = "Owns transaction states, isolation, MVCC, commit, rollback, and visibility rules. Use for transaction semantics and concurrency design."

[agents.transaction-wal-recovery-auditor]
config_file = "agents/transaction-wal-recovery-auditor.toml"
description = "Read-only auditor for transaction, WAL, MVCC, storage, and recovery invariants."

[agents.type-system-architect]
config_file = "agents/type-system-architect.toml"
description = "Owns scalar types, decimals, floats, text, enums, flags, nullability, StructuredObjects, and type contracts. Use for type policy, SRPL type binding, and contract-level type compatibility."

[agents.wal-implementation-engineer]
config_file = "agents/wal-implementation-engineer.toml"
description = "Implements WAL record, segment, replay, checksum, chain hash, and durability behavior."

[agents.wal-recovery-specialist]
config_file = "agents/wal-recovery-specialist.toml"
description = "Owns WAL records, replay, checkpoints, manifests, and crash-recovery scenarios. Use for log format, redo/undo behavior, recovery tests, and corruption handling."
