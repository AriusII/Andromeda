#![forbid(unsafe_code)]
#![doc = r#"
# andromeda-result-stream

Future owner for execution ResultStream sequencing.

This scaffold exports no runtime API yet. Promotion must prove metadata before
payload, bounded batches, terminal evidence validation, no business hardcoding,
and no application-facing SQL.
"#]
